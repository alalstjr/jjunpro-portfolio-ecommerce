<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jjunpro.shop.mapper.ProductOrderMapper">

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO
        product_order (ip, enabled, createddate, modifiedDate, orderName, orderEmail, orderPhone, postcode,
        addr1, addr2, memo, payment, cupon, point, totalAmount, productIds, productQuantitys, productAmounts, accountId)
    VALUES (
        #{ip},
        #{enabled},
        CURRENT_TIMESTAMP,
        CURRENT_TIMESTAMP,

        #{orderName},
        #{orderEmail},
        #{orderPhone},
        #{postcode},
        #{addr1},
        #{addr2},
        #{memo},

        #{payment},
        #{cupon},
        #{point},
        #{totalAmount},
        #{productIds},
        #{productQuantitys},
        #{productAmounts},
        #{accountId}
    );
  </insert>
</mapper>